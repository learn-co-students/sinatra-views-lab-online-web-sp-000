<!DOCTYPE html>
<html>
  <body>
        <h1>Today</h1>
        <% d = Date.today.strftime("%A, %e %B %Y").split %>
        <p><%= "The date is #{d[0]} #{d[2]} #{d[1]}, #{d[3]}" %></p>
  </body>      
</html>
