<!DOCTYPE html>
<html>
    <body>
        <h1>Today</h1>
       <% today = Time.now %>
       <p><%= "The date is #{today.strftime('%A')}, #{today.strftime('%B')} #{today.day}, #{today.year}" %></p>
    </body>
</html>