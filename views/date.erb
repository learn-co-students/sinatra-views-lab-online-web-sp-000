require "pry"

<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
      <h1>Today</h1>
      <p>
        
       <% d =  DateTime.now %>
        <% today = d.strftime('%A, %B %d, %Y') %>
        <%= "The date is" + " " + today %>
      </p>
      </body>
</html>


